<!-- Roadmap scaffold for the future paywall-detect package README. -->
# paywall-detect

`paywall-detect` is a planned standalone JavaScript library that will extract the paywall-detection logic from Paywall Bypass Script into a reusable package.

This package directory is a scaffold only. No public API has been finalized yet.
